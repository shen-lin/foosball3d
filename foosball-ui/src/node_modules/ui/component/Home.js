import React, { Component } from "react";
import "./Home.css";
import { connect } from "react-redux";

import NavCard from "ui/component/NavCard";
import Paper from "@material-ui/core/Paper";
import Grid from "@material-ui/core/Grid";

import ImageNewLobby from "ui/component/images/newlobby.jpg";
import ImageJoinGame from "ui/component/images/join.jpg";
import ImageLock from "ui/component/images/lock.png";

class Home extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <Grid container spacing={24} className="home-grid">
        <Grid item xs={8} className="home-grid-cell">
          <NavCard text="Join Game" image={ImageJoinGame} to="/joingame" pos="nav-card-centerbottom" stretch="nav-card-cover"/>
        </Grid>
        <Grid item xs={4} className="home-grid-cell">
          <NavCard text="Create Lobby" image={ImageNewLobby} to="/newlobby" pos="nav-card-centerbottom" stretch="nav-card-cover"/>
        </Grid>
        <Grid item xs={4} className="home-grid-cell">
          <NavCard text="Store" image={ImageLock} to="/" pos="nav-card-centercenter" stretch="nav-card-contain"/>
        </Grid>
        <Grid item xs={4} className="home-grid-cell">
          <NavCard text="Equipments" image={ImageLock} to="/" pos="nav-card-centercenter" stretch="nav-card-contain"/>
        </Grid>
        <Grid item xs={4} className="home-grid-cell">
          <NavCard text="Help" image={ImageLock} to="/" pos="nav-card-centercenter" stretch="nav-card-contain"/>
        </Grid>
      </Grid>
    );
  }
}

function mapStateToProps(state) {
  const { player } = state;

  return {
    player
  };
}

export default connect(mapStateToProps)(Home);
