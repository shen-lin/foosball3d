import { combineEpics } from "redux-observable";
import { createRoomEpic, joinRoomEpic, getRoomListEpic } from "epics/game";

export const rootEpic = combineEpics(createRoomEpic, joinRoomEpic, getRoomListEpic);
